@page "/{id:int?}"
@model Proiect_Nagy_Cristina.Pages.Childrens.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Children[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Children[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Children[0].Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Children[0].CNP)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Children[0].PhoneNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Children[0].Insurance)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Children[0].Doctor.Code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Children[0].Appointments)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ChildrenD.Childrens)
        {
            string selectedRow = "";
            if (item.ID == Model.ChildrenID)
            {
                selectedRow = "table-success";
            }
            <tr class="@selectedRow">
                <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Address)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CNP)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PhoneNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Insurance)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Doctor.Code)
                </td>
                <td>
                    @{ 
                        foreach(var availbility in  item.Appointments)
                        {
                            @availbility.AvailableTimeDate.ID @: @availbility.AvailableTimeDate.Availabilty <br />  
                        }
                    }
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
                </td>
            </tr>
            }
        </tbody>
</table>
